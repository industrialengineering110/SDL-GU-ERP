# PostgreSQL Connection String (from Render or Supabase)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret for Auth
JWT_SECRET=your_super_secret_key_here

# Port for the server (Render sets this automatically)
PORT=3000

# Node Environment
NODE_ENV=production
